import SwiftUI
import iosExport

struct Resource {
    static func colors() -> Colors {
        return LightColors()
    }
    
    static func typography() -> Typography {
        return Typography()
    }
}
